<h2>Formulario con Symfony</h2>

{% if (status != null)  and (data != null) %}
    <h3>{{status}}</h3>
    {{dump(data)}}
{% endif %}

{{ form_start(form, {'method':'POST'}) }}
<label>
    Campo titulo: 
    {{form_errors(form.titulo)}}
    {{form_widget(form.titulo)}}
</label>
{{ form_end(form) }}